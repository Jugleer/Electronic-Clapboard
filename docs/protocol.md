# Wire protocol — Electronic Clapboard v1

This document is the **contract** between the browser editor (`web/`) and the
ESP32-S3 firmware (`src/`). Subsequent build phases must not silently violate
it. If a change is genuinely needed, update this doc *first*, in the same PR
as the code change, and bump the protocol version.

**Protocol version:** `v1`
**Last reviewed:** Phase 0

---

## 1. Frame byte layout

The frame is the lingua franca of the system: 800×480 packed 1-bit pixels
shipped from browser to firmware as a raw binary blob.

### Dimensions and total size
| Field         | Value                |
| ------------- | -------------------- |
| Width         | `800` pixels         |
| Height        | `480` pixels         |
| Bits / pixel  | `1`                  |
| Bytes / row   | `100` (`800 / 8`)    |
| Total bytes   | **`48000`** (`100 × 480`) |

### Bit packing — locked
- **Pixels per byte:** 8.
- **Bit order within a byte:** **MSB first**. The leftmost pixel of a group of
  8 occupies bit 7 (`0x80`); the rightmost occupies bit 0 (`0x01`).
- **Byte order:** scanlines top-to-bottom; within a scanline, left-to-right.
- **Byte sense:** **`1 = ink (black)`**, `0 = paper (white)`.

This matches `tools/generate_slides.py:encode_1bpp_msb` and the GxEPD2
`drawInvertedBitmap()` call in firmware. **All-white → 48000 bytes of `0x00`.
All-black → 48000 bytes of `0xFF`.** This single property is the canary that
catches the most common wire-format bug (inverted bit sense); test it on
both ends.

### Coordinate convention
- Origin at **top-left**.
- `x` increases to the **right** (column index `0..799`).
- `y` increases **downward** (row index `0..479`).
- Matches HTML `<canvas>` and the e-paper panel as wired in
  [include/config.h](../include/config.h).

### Reference encoder
The Python module [tools/frame_format.py](../tools/frame_format.py) is the
authoritative spec implementation. The browser-side mirror lives at
[web/src/frameFormat.ts](../web/src/frameFormat.ts). Equivalence between them
is enforced by a committed binary fixture
(`web/src/__fixtures__/oracle_frame.bin`) generated by the Python module and
asserted byte-for-byte in `web/src/frameFormat.test.ts`. Any change to the
wire format must update both modules and regenerate the fixture in the same
commit.

---

## 2. HTTP endpoints

The ESP32 hosts an HTTP server on port **80** at hostname `clapboard.local`
(mDNS) or its raw DHCP IP. mDNS is unreliable on Windows without
Bonjour/iTunes installed; the editor must accept a manual host override.

All bodies and responses use `\r\n` line endings per RFC 7230. JSON responses
are UTF-8 with `Content-Type: application/json`.

### 2.1 `POST /frame` — push a frame to the panel

| Field            | Value                                 |
| ---------------- | ------------------------------------- |
| Method           | `POST`                                |
| Content-Type     | `application/octet-stream`            |
| Content-Length   | **must equal `48000`** exactly        |
| Body             | Raw 1-bit packed bytes per §1         |
| Query (optional) | `?full=1` forces full refresh         |

**Default refresh:** partial (faster, prone to ghosting after many updates).
**`?full=1`:** full refresh that clears ghosting (~1–4 s on this panel).

#### Response codes
| Status | Meaning                                                        | Body                          |
| ------ | -------------------------------------------------------------- | ----------------------------- |
| `200`  | Frame accepted and rendered (or render kicked off)             | JSON, see below               |
| `400`  | `Content-Length` present but ≠ 48000                           | JSON `{ "error": "..." }`     |
| `413`  | `Content-Length` > 48000 (hard reject before reading body)     | JSON `{ "error": "..." }`     |
| `415`  | Wrong or missing `Content-Type`                                | JSON `{ "error": "..." }`     |
| `503`  | A refresh is already in flight; client should retry            | JSON `{ "error": "..." }`     |

#### `200` body shape
```json
{
  "ok": true,
  "bytes": 48000,
  "render_ms": 1432,
  "full_refresh": false
}
```

#### Error body shape (any non-2xx)
```json
{
  "ok": false,
  "error": "human-readable reason",
  "code": "machine-readable slug"
}
```
Slugs lock in: `bad_size`, `too_large`, `bad_content_type`, `busy`,
`internal`. Add new slugs in this doc *before* the firmware emits them.

### 2.2 `GET /status` — health and last-frame metadata

```json
{
  "ok": true,
  "firmware_version": "0.1.0",
  "uptime_ms": 123456,
  "free_heap": 213456,
  "psram_free": 8000000,
  "last_frame_at": 122000,
  "last_frame_bytes": 48000,
  "last_frame_render_ms": 1432,
  "last_full_refresh": false
}
```

Before any frame has been received, the `last_frame_*` fields are `null`
(JSON null, not `0`).

### 2.3 `POST /sync` — fire LED + solenoid (Phase 7+)

Reserved. Body is empty. Response is JSON `{ "ok": true, "fired_at_ms": ... }`.
Returns `503` if battery is below `LOW_BATTERY_THRESHOLD_MV` from
[include/config.h](../include/config.h). Pulse durations are firmware-enforced
and not configurable over the wire in v1.

---

## 3. CORS

In **dev**, the firmware sends:
```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, OPTIONS
Access-Control-Allow-Headers: Content-Type
```
This is wide-open on purpose so the Vite dev server can hit the device
directly. **v2 will tighten this.** Adding the headers is a firmware
responsibility; if the browser silently drops responses, it almost always
means the headers are missing on a redirect or error path.

---

## 4. Timeouts and retry

| Operation          | Client timeout | Notes                                          |
| ------------------ | -------------- | ---------------------------------------------- |
| `GET /status`      | 3 s            | If exceeded, mark device unreachable.          |
| `POST /frame`      | 10 s           | Full refresh on this panel can take ~4 s.      |
| Retry on `503`     | back off       | First retry at 500 ms, then 1 s, then give up. |
| Retry on `5xx`     | once           | Don't hammer a wedged device.                  |
| Retry on network   | once           | mDNS/DHCP can blip during boot.                |

Clients **must not** retry on `4xx` — the request itself is wrong.

---

## 5. Versioning

Breaking changes bump the protocol to `v2`. Until then, `/status` may add
fields but must not remove them. New endpoints may appear; existing
endpoints' response shapes are stable.

If a future client cares about version, it should `GET /status` and look at
`firmware_version`. We may add a dedicated `protocol_version` field if/when
the schema diverges from firmware semver.

---

## 6. Things explicitly NOT in v1
- AP-mode captive portal for first-time Wi-Fi setup (creds live in
  gitignored `secrets.h` for now).
- Diff frames / partial region updates over the wire (we send the whole 48 KB).
- Authentication. The firmware is on a trusted LAN.
- Live-preview streaming while typing. Send is explicit per click.
- Multi-page templates / saved layouts on-device. Layouts live in browser
  IndexedDB.

---

## 7. Test obligations (referenced from the build plan)

- **Phase 0:** byte-for-byte parity between `tools/frame_format.py` and
  `web/src/frameFormat.ts` via committed fixture.
- **Phase 2:** all-white frame and all-black frame, end-to-end on hardware,
  before anything else. This catches inverted bit sense in one pass.
- **Phase 2:** boundary tests on `Content-Length` (47999 → 400, 48001 → 400
  or 413, missing → 411 or 400, wrong content-type → 415).
